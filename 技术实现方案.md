# iBanKa！云端部署器 - 技术实现方案

## 1. 架构设计与技术栈

本应用采用 **前端 Vanilla JS + 原生 CSS** 与 **后端 Node.js (ESM) / Cloudflare Pages Functions (`_worker.js`)** 双端架构。

- **前端 (Frontend)**:
  - HTML5 + Modern CSS Flex/Grid 响应式设计（支持单版面自适应与无滚动条排版）。
  - Vanilla JS，支持中英文双语界面快速切换、无滚动条适配与响应式布局。
  - AI API 设置模态框：支持本地存储（LocalStorage）保存用户自定义的 OpenAI 兼容 API 接口（Base URL, API Key, Model）或系统默认 Gemini API。

- **后端 (Backend & Proxy)**:
  - Node.js ESM 原生 HTTP 服务器 (`server.mjs` + 模块化解耦模块 `lib/cf-api.js` 与 `lib/deploy-engine.js`)。
  - Cloudflare Pages Functions (`functions/api/[[path]].js`) 适配 Worker 环境部署。
  - 文件解耦限制：保证所有代码文件行数严格控制在 600 行以内。

## 2. 凭据校验与多鉴权模式 (API Token & Global API Key)

去除了原代码中对 37 位 Global API Key 的强制正则校验与邮箱必填校验，实现了对 **Cloudflare API Token** 与 **Global API Key** 的无缝双模式支持：

1. **凭据识别逻辑**:
   - 若用户输入的 Key 符合 37 位 16 进制字符串格式且填有邮箱，系统自动采用 **Global API Key 模式**（请求头使用 `X-Auth-Email` + `X-Auth-Key`，Wrangler 环境变量使用 `CLOUDFLARE_EMAIL` + `CLOUDFLARE_API_KEY`）。
   - 若邮箱为空或 Key 不满足 37 位格式，系统自动识别为 **API Token 模式**（请求头使用 `Authorization: Bearer <Token>`，Wrangler 环境变量使用 `CLOUDFLARE_API_TOKEN`）。

2. **Wrangler 部署与多线程调优**:
   - 自动生成临时项目并构建 Pages/Worker 捆绑代码。
   - 适配 JWT Upload Token 机制实现零外部依赖直接推送到 Pages Assets/Deployments 接口。

## 3. AI API 设置与免费获取指南

- 提供内置 AI 配置管理：
  - 默认模式：系统内置 Gemini API KEY
  - 自定义模式：支持填写 OpenAI 兼容 Base URL、API Key 与 Model 名称，参数持久化存储于 `localStorage`。
- 「免费 AI API 获取指南」弹窗：详细说明主流免费 API (如 Google Gemini API, Groq, SiliconFlow, DeepSeek 等) 的获取方式、优势对比与配置参数范例。
