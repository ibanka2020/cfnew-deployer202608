# iBanKa！云端部署器 - 产品设计文档

## 1. 产品定位与核心价值

**iBanKa！云端部署器** 是一款轻量、安全、高效的 Cloudflare Pages & Workers 一键部署工具。无需复杂的本地开发环境或配置 CLI，只需提供 Cloudflare 凭据（支持 API Token 或 邮箱 + Global API Key），点击一次即可自动完成项目创建、UUID 配置、KV 绑定以及域名挂载。

## 2. 主要功能特性

1. **灵活的凭据鉴权 (Token & Global Key)**
   - 全面移除针对 Key 格式的硬编码 37 位正则校验限制。
   - 完美支持 Cloudflare 推荐的细粒度 API Token（邮箱可选）以及传统 Global API Key。

2. **一键快速部署 (Quick Deploy)**
   - 自动生成随机 UUID、随机 KV 空间及随机 Cloudflare Pages 项目。
   - 自动绑定指定 Cloudflare 区域下的随机子域名（可选）。

3. **高级配置与更新模式 (Advanced Setup & Update)**
   - 支持自定义选择 Worker 或 Pages 部署方式。
   - 支持加密/混淆源或明文源切换。
   - 支持读取账号内现有 Pages/Worker 项目及 KV 空间，提供代码同步更新模式（保持原 KV 与 UUID 绑定不变）。

4. **双语界面与单版面适配 (Bilingual UI & Single Screen)**
   - 支持中英文界面快速一键切换，切换按钮位于页面右上角。
   - 单版面（Single Page / One-Screen）无滚动条视觉排版，适配 Desktop 及 Mobile 设备。

5. **AI API 设置与获取指南**
   - 界面内置「AI API 设置」面板，支持用户在系统默认 Gemini API 与自定义 OpenAI 兼容接口之间灵活切换。
   - 提供「免费 AI API 获取指南」弹窗，涵盖优缺点对比及配置示例。

## 3. 用户界面设计规范

- **应用及浏览器标题**: 前缀统一加 `iBanKa！`（如 `iBanKa！云端部署器`）。
- **色调与风范**: 采用清新柔和的薄荷绿/天蓝色调，毛玻璃质感面板搭配高对比度文字。
- **右上角操作区**: 语言切换按钮铺开展示（[ 中文 ] / [ English ]）及「AI API 设置」按钮。
